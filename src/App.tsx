//* src/App.tsx

import AppRoutes from "@/routes/AppRoutes";

/**
 * Root application component — renders the route tree.
 * Providers are set up in main.tsx.
 */
const App = () => {
	return <AppRoutes />;
};

export default App;
